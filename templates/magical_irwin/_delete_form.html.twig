<form method="post" action="{{ path('app_magical_i_r_w_i_n_delete', {'id': wish.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ wish.id) }}">
    <button class="btn">Delete</button>
</form>
